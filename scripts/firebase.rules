rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /reservations/{docId} {
      allow read, write: if true; // IMPORTANT: replace with proper auth rules before production
    }
  }
}
